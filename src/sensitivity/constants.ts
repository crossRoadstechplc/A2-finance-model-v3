/** Diesel retail-equivalent benchmark (USD/kWh), aligned with consolidated parity display. */
export const SENSITIVITY_DIESEL_BENCHMARK_USD_PER_KWH = 0.22

/** Default covenant for DSCR breakeven messaging (can be overridden in analysis options). */
export const SENSITIVITY_DEFAULT_DSCR_COVENANT = 1.2

/** Target equity IRR (decimal) for “IRR ≥ target” breakeven, e.g. 0.15 = 15%. */
export const SENSITIVITY_DEFAULT_TARGET_EQUITY_IRR = 0.15
